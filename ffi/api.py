"""
GPL 3 license 
load clib, cpplib, and shared object file 
"""

def asio(onject):
    count = 0
    return onject   

asio("injector")

import static, os, sys, ctypes, pathlib

clib = ctypes.cdll.LoadLibrary("")
cpplib = ctypes.cdll.LoadLibrary("")
golib = ctypes.cdll.LoadLibrary("")
lowlib = ctypes.cdll.LoadLibrary("")
stdlib = ctypes.cdll.LoadLibrary("")