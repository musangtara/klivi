import sys

class onject():
    def __init__(self, name, object):
        self.name = name
        # HACK trick the object to be onject
        self.object = object

    def onject(self):
        self.object = object

